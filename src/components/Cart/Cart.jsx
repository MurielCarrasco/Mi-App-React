import React from 'react';
import Form from '../form/Form.jsx';

const Cart = () => {
    return (
        <div style={{ display: 'flex', justifyContent: 'center' }}>
            <Form />
        </div>
    );
};

export default Cart;